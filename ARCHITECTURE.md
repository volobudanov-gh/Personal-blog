# 🏗️ Архитектура проекта Personal Blog

**Дата создания:** 6 мая 2026  
**Проект:** Personal Blog (Персональный блог на испанском)  
**Цель:** Персональная презентация + поиск партнера через тест совпадения интересов  
**Стек:** Next.js + Supabase + Vercel + GitHub

---

## 📋 Содержание

1. [Цель проекта](#цель-проекта)
2. [Архитектура системы](#архитектура-системы)
3. [Технический стек](#технический-стек)
4. [Структура БД](#структура-бд)
5. [Структура проекта](#структура-проекта)
6. [Процесс разработки](#процесс-разработки)
7. [Как работать с Claude](#как-работать-с-claude)

---

## 🎯 Цель проекта

### Основная цель
- Создать персональный блог-презентацию на **испанском языке**
- Показать себя таким, какой я есть на самом деле
- Найти партнера (парня) для создания семьи

### Целевая аудитория
- Потенциальные романтические партнеры
- Люди с совпадающими интересами и ценностями

### Основные функции
1. **Профиль** - статическое описание Voldy с данными
2. **Блог** - статьи и публикации (публичные и приватные)
3. **Тест совпадения** - определение совместимости с посетителями
4. **Админ-панель** - управление контентом (только для автора)

---

## 🏗️ Архитектура системы

```
┌─────────────────────────────────────────────────────────┐
│                    ПОСЕТИТЕЛЬ (Frontend)                │
│  (Браузер) → Next.js App → React Components             │
└────────────────────────┬────────────────────────────────┘
                         │ HTTP/REST API
┌────────────────────────▼────────────────────────────────┐
│                   SUPABASE (Backend)                     │
│  ┌──────────────────────────────────────────────────┐  │
│  │          PostgreSQL Database                      │  │
│  │  ┌──────────────────┐   ┌──────────────────┐     │  │
│  │  │ profile_sections │   │ profile_fields   │     │  │
│  │  │ (5 разделов)     │   │ (20 полей)       │     │  │
│  │  └──────────────────┘   └──────────────────┘     │  │
│  │                                                    │  │
│  │  Дополнительно (будет):                          │  │
│  │  - posts (статьи блога)                          │  │
│  │  - test_questions (вопросы теста)                │  │
│  │  - test_results (результаты тестов)              │  │
│  │  - photos (фотографии профиля)                   │  │
│  └──────────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────┐  │
│  │       Supabase Auth (аутентификация)              │  │
│  │  (для приватного кабинета автора)                │  │
│  └──────────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────┐  │
│  │      Supabase Storage (хранилище файлов)         │  │
│  │  (фотографии, медиа)                             │  │
│  └──────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                         │
        ┌────────────────┼────────────────┐
        │                │                │
┌───────▼──────┐ ┌──────▼───────┐ ┌─────▼──────────┐
│ GitHub       │ │ Vercel       │ │ npm/Packages   │
│ (версионирование,│ (хостинг,   │ (зависимости)  │
│  миграции БД,    │  CI/CD,      │                │
│  код)            │  autodeploy) │                │
└────────────────┘ └──────────────┘ └────────────────┘
```

---

## 🛠️ Технический стек

### Frontend
- **Next.js 14+** - React фреймворк с SSR/SSG
- **TypeScript** - типизированный JavaScript
- **Tailwind CSS** - утилитарный CSS
- **React** - компоненты UI

### Backend
- **Supabase** - Firebase альтернатива
  - PostgreSQL база данных
  - REST API (автоматически генерируется из таблиц)
  - Аутентификация (Supabase Auth)
  - Хранилище файлов (Storage)
  - Real-time подписки (опционально)

### DevOps & CI/CD
- **GitHub** - версионирование кода и миграций
- **Vercel** - хостинг Next.js приложения
- **GitHub Actions** - автоматизация (опционально)

### Языки
- **Испанский** - для блога и пользовательского контента
- **Английский** - для админ-панели и системных сообщений

---

## 💾 Структура БД

### Текущее состояние (✅ создано)

#### Таблица: `profile_sections`
Разделы профиля для организации информации.

| Колонка | Тип | Описание |
|---------|-----|---------|
| id | BIGSERIAL | Уникальный ID |
| section_name_es | TEXT | Название раздела на испанском |
| section_name_en | TEXT | Название раздела на английском |
| order | INTEGER | Порядок отображения |
| icon | TEXT | Иконка для UI |
| created_at | TIMESTAMP | Дата создания |

**Данные:**
1. Información Básica
2. Características Físicas
3. Astrología
4. Estilo de Vida
5. Personalidad y Educación

#### Таблица: `profile_fields`
Поля профиля с данными автора.

| Колонка | Тип | Описание |
|---------|-----|---------|
| id | BIGSERIAL | Уникальный ID |
| field_name | TEXT | Техническое имя поля (уникальное) |
| label_es | TEXT | Название на испанском |
| label_en | TEXT | Название на английском |
| section_id | BIGINT | Ссылка на раздел (FK) |
| data_type | TEXT | Тип данных (text, integer, boolean) |
| value | TEXT | Текущее значение |
| order | INTEGER | Порядок в разделе |
| created_at | TIMESTAMP | Дата создания |

**Текущие поля (20):**
- Básica: nombre, edad, nacionalidad, ciudad, profesion
- Física: altura, peso, tipo_cuerpo, color_ojos, cabello
- Astrología: signo_zodiaco_mes, signo_zodiaco_chino
- Lifestyle: fumar, alcohol, estado_civil, situacion_vivienda, licencia_conducir, experiencia_relaciones_pasadas
- Personalidad: tipo_personalidad, educacion

### Планируемые таблицы (🔄 будут созданы)

#### `posts`
Статьи и посты блога.

```sql
CREATE TABLE posts (
  id BIGSERIAL PRIMARY KEY,
  title TEXT,
  content TEXT,
  category TEXT,
  is_public BOOLEAN,
  created_at TIMESTAMP,
  updated_at TIMESTAMP
);
```

#### `test_questions`
Вопросы для теста совпадения интересов.

```sql
CREATE TABLE test_questions (
  id BIGSERIAL PRIMARY KEY,
  question_es TEXT,
  question_en TEXT,
  type TEXT, -- 'yes_no', 'multiple_choice', 'scale'
  options JSONB,
  category TEXT,
  order INTEGER
);
```

#### `test_results`
Результаты пройденных тестов посетителями.

```sql
CREATE TABLE test_results (
  id BIGSERIAL PRIMARY KEY,
  user_name TEXT,
  email TEXT,
  answers JSONB,
  score INTEGER,
  created_at TIMESTAMP
);
```

#### `photos`
Фотографии профиля.

```sql
CREATE TABLE photos (
  id BIGSERIAL PRIMARY KEY,
  profile_id BIGINT,
  photo_url TEXT,
  is_main BOOLEAN,
  order INTEGER,
  uploaded_at TIMESTAMP
);
```

---

## 📁 Структура проекта

```
Personal-blog/
├── .gitignore                      # Git конфиг
├── README.md                       # Описание проекта
├── ARCHITECTURE.md                 # Этот файл - архитектура
│
├── package.json                    # Зависимости Node
├── tsconfig.json                   # Конфиг TypeScript
├── next.config.js                  # Конфиг Next.js
│
├── supabase/
│   ├── .env.local                  # Локальные переменные Supabase
│   └── migrations/
│       └── 20260506000001_create_profile_tables.sql
│
├── app/                            # Next.js App Router
│   ├── layout.tsx                  # Главный макет
│   ├── page.tsx                    # Главная страница (профиль)
│   ├── blog/
│   │   ├── page.tsx               # Список статей
│   │   └── [slug]/page.tsx        # Одна статья
│   ├── test/
│   │   └── page.tsx               # Тест совпадения
│   └── admin/
│       ├── layout.tsx             # Админ макет (protected)
│       ├── page.tsx               # Админ главная
│       ├── profile/page.tsx       # Редактирование профиля
│       ├── posts/page.tsx         # Управление статьями
│       └── results/page.tsx       # Результаты тестов
│
├── components/                     # React компоненты
│   ├── ProfileCard.tsx             # Карточка профиля
│   ├── BlogPost.tsx                # Компонент статьи
│   ├── TestForm.tsx                # Форма теста
│   ├── AdminHeader.tsx             # Админ хедер
│   └── Navigation.tsx              # Навигация
│
├── lib/                            # Утилиты и помощники
│   ├── supabase.ts                 # Supabase клиент
│   ├── utils.ts                    # Общие утилиты
│   └── auth.ts                     # Аутентификация
│
├── styles/                         # CSS стили
│   ├── globals.css                 # Глобальные стили
│   └── variables.css               # CSS переменные
│
├── public/                         # Статические файлы
│   ├── images/
│   ├── icons/
│   └── fonts/
│
└── .env.local                      # Переменные окружения (в .gitignore)
    # NEXT_PUBLIC_SUPABASE_URL=...
    # NEXT_PUBLIC_SUPABASE_ANON_KEY=...
```

---

## 🔄 Процесс разработки

### Фаза 1: ✅ Инициализация (ЗАВЕРШЕНА)
- [x] Создание GitHub репозитория
- [x] Создание Supabase проекта
- [x] Создание структуры БД (profile_sections, profile_fields)
- [x] Добавление SQL миграций в GitHub
- [x] Заполнение данных профиля Voldy

### Фаза 2: 🔄 Фронтенд основы (СЛЕДУЮЩАЯ)
- [ ] Инициализация Next.js проекта
- [ ] Установка зависимостей (Supabase, Tailwind)
- [ ] Подключение Supabase клиента
- [ ] Создание главной страницы с профилем
- [ ] Создание навигации

### Фаза 3: 📝 Функциональность профиля
- [ ] Создание компонента ProfileCard
- [ ] Отображение всех 20 полей профиля
- [ ] Красивый UI/UX дизайн
- [ ] Многоязычная поддержка (ES/EN)
- [ ] Загрузка фотографий

### Фаза 4: 🧪 Тест совпадения
- [ ] Создание таблицы test_questions
- [ ] Создание компонента TestForm
- [ ] Логика подсчёта совпадения
- [ ] Сохранение результатов (test_results)
- [ ] Админ-панель для просмотра результатов

### Фаза 5: 📰 Блог
- [ ] Создание таблицы posts
- [ ] Компоненты для отображения статей
- [ ] Админ-панель для создания/редактирования
- [ ] Приватные и публичные посты
- [ ] Категоризация и поиск

### Фаза 6: 🔐 Аутентификация и админ-панель
- [ ] Настройка Supabase Auth
- [ ] Защита приватного кабинета
- [ ] Полная админ-панель
- [ ] Управление контентом

### Фаза 7: 🚀 Развёртывание
- [ ] Подключение Vercel
- [ ] Настройка CI/CD
- [ ] Переменные окружения на Vercel
- [ ] Тестирование на production
- [ ] Запуск!

---

## 👨‍💻 Как работать с Claude

### Принципы работы

#### 1. **Шаг за шагом, без спешки**
- Не спешим с добавлением всех функций сразу
- Завершаем одну фазу перед следующей
- Проверяем результат перед продолжением

#### 2. **Ясность и точность**
- Описываем проблему/задачу подробно
- Задаём уточняющие вопросы если что-то непонятно
- Говорим "хватит" когда нужна пауза

#### 3. **Документирование**
- Каждый важный шаг записывается
- Архитектурные решения объясняются
- Обновляем этот документ при изменениях

### Как запросить помощь

**Плохо:**
```
"Создай блог"
```

**Хорошо:**
```
"Создай главную страницу, которая:
- Отображает профиль Voldy
- Показывает все 20 полей из profile_fields
- Группирует поля по разделам (profile_sections)
- Работает на испанском и английском"
```

### Команды для Claude

| Команда | Использование |
|---------|---------------|
| "Начни с фазы X" | Переход к следующей фазе разработки |
| "Медленнее" | Замедлить темп, больше объяснений |
| "Спешим" | Быстрее, меньше деталей |
| "Проверь архитектуру" | Рассказать о текущей архитектуре |
| "Обнови документ" | Изменить ARCHITECTURE.md |
| "Стоп, не спеши" | Остановиться и ждать инструкций |

### Важные переменные

```
# .env.local (не коммитить в Git!)
NEXT_PUBLIC_SUPABASE_URL = "https://prIdmdadnotydwiodwti.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY = "[anon_key_from_supabase_settings]"
SUPABASE_SERVICE_ROLE_KEY = "[service_role_key_from_supabase_settings]"
```

**⚠️ ВАЖНО:** Никогда не коммитьте следующие данные в Git:
- Service Role Key
- Database Password
- GitHub Personal Access Token

Используйте `.env.local` файл (добавлен в `.gitignore`) для локальной разработки.
Для Vercel используйте Project Settings → Environment Variables.

### Контакты и аккаунты

| Сервис | Username | URL |
|--------|----------|-----|
| GitHub | volobudanov-gh | https://github.com/volobudanov-gh/Personal-blog |
| Supabase | volobudanov | https://supabase.com/projects |
| Vercel | - | https://vercel.com |

---

## 📊 Текущий статус

**Дата последнего обновления:** 6 мая 2026

### Завершено ✅
- [x] Определена архитектура системы
- [x] Создан GitHub репозиторий
- [x] Создан Supabase проект
- [x] Спроектирована структура БД
- [x] Созданы SQL миграции
- [x] Заполнены данные профиля Voldy (20 полей)
- [x] Документирована архитектура

### В процессе 🔄
- [ ] Ничего (ждём инструкций)

### Готово к началу 🚀
- [ ] Инициализация Next.js проекта
- [ ] Создание фронтенда профиля

---

## 🔗 Важные ссылки

- **GitHub репозиторий:** https://github.com/volobudanov-gh/Personal-blog
- **Supabase Dashboard:** https://supabase.com/projects
- **Vercel Dashboard:** https://vercel.com
- **SQL миграции:** `/supabase/migrations/20260506000001_create_profile_tables.sql`

---

## 📝 Примечания

### Почему такая архитектура?

1. **Next.js** - SSR/SSG для лучшей SEO, удобство разработки
2. **Supabase** - легко масштабировать, встроенная аутентификация, real-time возможности
3. **Vercel** - идеален для Next.js, автоматический CI/CD
4. **GitHub** - версионирование миграций БД важно

### Безопасность

- Приватный кабинет будет защищён Supabase Auth
- Service Role Key хранится только на сервере (backend)
- Anon Key используется на фронтенде (ограниченные права)
- Данные пользователей (email, результаты тестов) не экспортируются

### Масштабируемость

Архитектура позволяет легко:
- Добавить больше постов и статей
- Расширить тест новыми вопросами
- Добавить комментарии, рейтинги
- Интегрировать социальные сети
- Запустить мобильное приложение

---

**Создано:** 6 мая 2026  
**Автор:** Claude + Voldy  
**Версия:** 1.0  
**Статус:** Активная разработка
